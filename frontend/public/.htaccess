Options -MultiViews

RewriteEngine On

# Serve real assets directly and send client-side routes to the Vue entry point.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
RewriteRule ^ index.html [L]
