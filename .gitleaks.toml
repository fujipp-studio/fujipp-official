[extend]
useDefault = true

[[allowlists]]
description = "Discord application and guild IDs in API documentation are public identifiers"
targetRules = ["discord-client-id"]
condition = "AND"
regexTarget = "line"
regexes = ['''"discord(?:Application|Guild)Id"\s*:\s*"[0-9]{15,30}"''']
paths = ['''docs/store-api\.md''']
